import { EntityAddress, TransactionManifest } from "../../models";
export declare class AnalyzeManifestRequest {
    networkId: number;
    transactionManifest: TransactionManifest;
    constructor(networkId: number, transactionManifest: TransactionManifest);
    toString(): string;
    toObject(): Record<string, any>;
}
export declare class AnalyzeManifestResponse {
    packageAddresses: Array<EntityAddress.PackageAddress>;
    componentAddresses: Array<EntityAddress.ComponentAddress>;
    resourceAddresses: Array<EntityAddress.ResourceAddress>;
    accountAddresses: Array<EntityAddress.ComponentAddress>;
    accountsRequiringAuth: Array<EntityAddress.ComponentAddress>;
    accountsWithdrawnFrom: Array<EntityAddress.ComponentAddress>;
    accountsDepositedInto: Array<EntityAddress.ComponentAddress>;
    constructor(packageAddresses: Array<EntityAddress.PackageAddress>, componentAddresses: Array<EntityAddress.ComponentAddress>, resourceAddresses: Array<EntityAddress.ResourceAddress>, accountAddresses: Array<EntityAddress.ComponentAddress>, accountsRequiringAuth: Array<EntityAddress.ComponentAddress>, accountsWithdrawnFrom: Array<EntityAddress.ComponentAddress>, accountsDepositedInto: Array<EntityAddress.ComponentAddress>);
    toString(): string;
    toObject(): Record<string, any>;
}
