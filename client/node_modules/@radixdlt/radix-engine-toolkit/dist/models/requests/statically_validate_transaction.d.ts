export declare class ValidationConfig {
    networkId: number;
    minCostUnitLimit: number;
    maxCostUnitLimit: number;
    minTipPercentage: number;
    maxTipPercentage: number;
    maxEpochRange: number;
    constructor(networkId: number, minCostUnitLimit: number, maxCostUnitLimit: number, minTipPercentage: number, maxTipPercentage: number, maxEpochRange: number);
    static default(networkId: number): ValidationConfig;
    toString(): string;
    toObject(): Record<string, any>;
}
export declare class StaticallyValidateTransactionRequest {
    compiledNotarizedIntent: Uint8Array;
    validationConfig: ValidationConfig;
    constructor(compiledNotarizedIntent: Uint8Array | string, validationConfig: ValidationConfig);
    toString(): string;
    toObject(): Record<string, any>;
}
export declare class StaticallyValidateTransactionResponse {
    readonly validity: "Valid" | "Invalid";
    constructor(validity: "Valid" | "Invalid");
}
export declare class StaticallyValidateTransactionResponseValid extends StaticallyValidateTransactionResponse {
    constructor();
    toString(): string;
    toObject(): Record<string, any>;
}
export declare class StaticallyValidateTransactionResponseInvalid extends StaticallyValidateTransactionResponse {
    error: string;
    constructor(error: string);
    toString(): string;
    toObject(): Record<string, any>;
}
