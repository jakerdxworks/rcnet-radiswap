import { EntityAddress, PublicKey } from "..";
export declare class DeriveBabylonAddressFromOlympiaAddressRequest {
    networkId: number;
    olympiaAccountAddress: string;
    constructor(networkId: number, olympiaAccountAddress: string);
    toString(): string;
    toObject(): Record<string, any>;
}
export declare class DeriveBabylonAddressFromOlympiaAddressResponse {
    babylonAccountAddress: EntityAddress.ComponentAddress;
    publicKey: PublicKey.PublicKey;
    constructor(babylonAccountAddress: EntityAddress.ComponentAddress, publicKey: PublicKey.PublicKey);
    toString(): string;
    toObject(): Record<string, any>;
}
