import { EntityAddress } from "../../";
export declare class DecodeAddressRequest {
    address: string;
    constructor(address: string);
}
export declare class DecodeAddressResponse {
    networkId: number;
    networkName: string;
    entityType: EntityAddress.EntityType;
    data: Uint8Array;
    hrp: string;
    constructor(networkId: number, networkName: string, entityType: EntityAddress.EntityType, data: Uint8Array | string, hrp: string);
    toString(): string;
    toObject(): Record<string, any>;
}
