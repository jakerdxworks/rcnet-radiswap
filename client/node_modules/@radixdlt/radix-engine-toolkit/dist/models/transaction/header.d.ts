import { PublicKey } from "../crypto";
/**
 * A transaction header containing metadata and other transaction information.
 */
export declare class TransactionHeader {
    version: number;
    networkId: number;
    startEpochInclusive: number;
    endEpochExclusive: number;
    nonce: number;
    notaryPublicKey: PublicKey.PublicKey;
    notaryAsSignatory: boolean;
    costUnitLimit: number;
    tipPercentage: number;
    constructor(version: number, networkId: number, startEpochInclusive: number, endEpochExclusive: number, nonce: number, notaryPublicKey: PublicKey.PublicKey, notaryAsSignatory: boolean, costUnitLimit: number, tipPercentage: number);
    toString(): string;
    toObject(): Record<string, any>;
}
